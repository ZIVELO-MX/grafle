# Allow navigating to future puzzles (beyond today's date)
# Default: false
VITE_ALLOW_FUTURE_PUZZLES=false

# Skip the start screen and go straight into the puzzle
# Default: false (start screen is shown)
VITE_SKIP_START_SCREEN=false
